class Group2 {
    constructor (
        id,
        groupName,
        image
    ) {
        this.id = id;
        this.groupName = groupName;
        this.image = image;
    }
}

export default Group2;